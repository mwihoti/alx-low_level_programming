#include "main.h"
/**
 * _abs -c computes the absolute value of integer
 * @n: the int to check
 *
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
